import MajorSection from "../MajorSection";

const PersonalDetails = (props) => {
    return (
        <MajorSection id='personal-details' sectionStyle={props.sectionStyle}>
            personal details
        </MajorSection>
    );
}

export default PersonalDetails;
