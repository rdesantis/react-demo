import Nav from './Nav';

function Header() {
    return (
        <header>
            <img src='Horizontal logo.png' alt='logo' width='100'/>
            <Nav/>
        </header>
    );
};

export default Header;
