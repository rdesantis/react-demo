import Nav from './Nav';

function Header() {
    return (
        <header>
            <img src='Horizontal logo.png' alt='logo'/>
            <Nav/>
        </header>
    );
};

export default Header;
