import MajorSection from "../MajorSection";

const ReservationBasics = (props) => {
    return (
        <MajorSection id='reservation' sectionStyle={props.sectionStyle}>
            reservation basics
        </MajorSection>
    );
}

export default ReservationBasics;
