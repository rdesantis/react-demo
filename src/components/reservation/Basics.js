import MajorSection from "../MajorSection";

const ReservationBasics = () => {
    return (
        <MajorSection sectionStyle='reservation'>
            reservation basics
        </MajorSection>
    );
}

export default ReservationBasics;
