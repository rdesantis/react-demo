import MajorSection from "../MajorSection";

const About = (props) => {
    return (
        <MajorSection id='about' sectionStyle={props.sectionStyle}>
            about
        </MajorSection>
    );
}

export default About;
